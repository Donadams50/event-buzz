import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#4F6F9E', 
        height: 50,
        flexDirection: 'row',
        width: '100%',
        justifyContent: 'center'
      },
      iconContainer: {
        width: 75, 
        height: 50, 
        marginHorizontal: 5,
      },
     
      icon: {
        width: '100%',
        height: '100%',
      }
});

export default styles;
